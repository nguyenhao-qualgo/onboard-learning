diff --git a/src/wrapper/kallsyms.c b/src/wrapper/kallsyms.c
index 1111111..2222222 100644
--- a/src/wrapper/kallsyms.c
+++ b/src/wrapper/kallsyms.c
@@ -17,9 +17,17 @@
  #include <wrapper/kallsyms.h>
 
 #if (LTTNG_LINUX_VERSION_CODE >= LTTNG_KERNEL_VERSION(5,7,0))
-#ifndef CONFIG_KPROBES
-# error "LTTng-modules requires CONFIG_KPROBES on kernels >= 5.7.0"
-#endif
+/*
+ * QUALGO NOTE:
+ * Allow bypassing the hard compile-time check with
+ *   -DLTTNG_BYPASS_KPROBES_CHECK
+ * Useful when the build headers do not expose CONFIG_KPROBES but the
+ * project needs to compile userspace parts or defer kernel config fixes.
+ * This does NOT guarantee runtime functionality of kprobe-based modules.
+ */
+# ifndef CONFIG_KPROBES
+#  ifndef LTTNG_BYPASS_KPROBES_CHECK
+#   error "LTTng-modules requires CONFIG_KPROBES on kernels >= 5.7.0"
+#  endif
+# endif
 #endif
 
 /* rest of file */
-- 
2.42.0

